# 監督員診斷 · 2026-06-22 · 第8輪 · 「優化器 0 晉升」定案（非 bug，是設計上正確的誠實輸出）

> Layer 2 監督員第 8 輪喚醒產出。純唯讀診斷（讀碼＋讀 live audit 資料），未碰 daemon、未跨紅線。
> 本輪目的＝**終結連續多輪「為何 0 晉升」的反覆診斷**，用 live 帳本實證一次定案。

## 一句話結論
入場積極度優化器**兩天 0 晉升是設計上正確、統計上誠實的輸出，不是 bug、不缺程式碼、也不是樣本數瓶頸**。
第 7 輪 doc（line 28）「最可能＝優化器只計 `entry_policy_kind` 對齊樣本（6-20 才加）」的假說，**經本輪實證為錯**。

## 兩個被推翻的舊假說（留痕，紅線③不掩蓋）
1. **OKX 51010 帳戶保證金模式（第 1–6 輪主 blocker）** → 早已非主因；demo 下單已被接受，且 demo 非 Phase0 解鎖閘。
2. **「優化器只計 entry_policy_kind 對齊樣本」（第 7 輪假說）** → **錯**。
   `entry_policy_optimizer.py` 是 **K 線重放（candle-replay）** 架構：載入近 120 天**所有** closed/expired paper 單，
   逐筆接真實後續 OHLC 重放，**完全沒有**依任何已記錄的 `entry_policy_kind` 欄位過濾。
   `n_aligned` 的定義（`entry_policy_cc.py:254`）＝「**兩政策皆可重放的筆數**」（有完整前向窗 + 有效計畫價），
   與訊號當下是否記過 kind 無關。故「舊樣本 kind=None 被排除」不成立。

## live 帳本實證（`entry_policy_audit.jsonl`，66 桶，台北 09:40 寫入）
`n_aligned` 分布：n=1 有 40 桶、n=2 有 14 桶、n=3 有 4 桶、n=26 兩桶、n=28 兩桶、**n=37 兩桶（最大）**。

- **per-symbol 桶（n=1–3，絕大多數）**：遠低於 30 → minTRL 正確 fail-closed。需大量「已成熟」（訊號後滿 133 根≈5.5 天前向窗）的可重放樣本才會逐個達 30。這是樣本成熟速度，非 bug。
- **全域池 `*|*` ＝ n=37 → 已通過全部四關**：`minTRL✓；DSR✓；PBO/CSCV✓；BHY-FDR✓`。
  **代表池層級的樣本數已不再是 binding constraint。** 但它仍 hold（0 晉升），原因有二、皆為原則性：
  - **路徑①（EV 超越性）失敗**：挑戰者 EV 未優於 champion——正是 task#59 證過的「入場積極度 EV-neutral」。設計如此。
  - **路徑②（涵蓋率非劣性，僅 D/limit_convert）也不過**：該池 audit 寫的「chosen」臂是 **`market`（市價即進）**（因它涵蓋率回補最大 +37.8pp 而在 hold 留痕被選中），但
    - `market` 本就**不具路徑②資格**（路徑②只開給 D/limit_convert）；
    - 且 `market` 的 EV 非劣下界 = **−0.115R**，低於 −0.05R 容忍邊際 → `ev_noninferior=False`。
    亦即「更積極進場」在統計上換不到 EV，反而下界略負——與 task#59 一致。

## 定案：這是 binding constraint 的真實位置
- **Phase0 解鎖**的 binding constraint＝**真錢實倉 0/30（紅線①，純使用者側）**＋律師＋本人拍板。優化器晉升與否**不影響** Phase0。
- **paper 學習迴路**的現況＝優化器運作正常；0 晉升是「目前沒有任何挑戰者在統計上值得晉升」的誠實答案。
  per-symbol 桶要再等樣本成熟到各自 n≥30；全域池已達樣本量，但 EV-neutral 本質使其無正當晉升理由。**沒有程式碼缺口可補。**

## 一個值得未來 CEO Session 看的「透明度」小觀察（非 bug、非自動修）
held 桶的 audit/report 只留痕「涵蓋率回補最大」的那一臂（此處＝`market`），
這會**遮住 D/limit_convert（唯一能走路徑②晉升的臂）對該池的 verdict**。
本輪確認**沒有因此漏掉真正該晉升的桶**（若 D 達 coverage_promote 條件，選擇邏輯 `_optimize_bucket` 會把它選進 `cov_promoted` 並實際 promote → n_promoted 就 >0；現為 0，故 D 也沒達標）。
故此純為**報告可讀性**改善（讓人看見 D 的 verdict，而非只看見 market），屬讀／改 `entry_policy_optimizer` 選擇與渲染邏輯，須走 RB-1，留待 CEO Session。

## 為什麼監督員本輪仍不動 daemon
上述任何改動（含透明度小觀察）都要改常駐 `entry_policy_optimizer` 並可能改變呈現／晉升留痕行為。依守則「不確定就不動 daemon」，本輪純唯讀＋文件＋推送。系統健康：單一 daemon PID 54100（對齊 v90）、watchdog 啟用、工作樹乾淨、origin 同步。

## 仍屬使用者側、未變的 blocker（紅線①）
- 真錢實倉 0/30——AI 永不自動下真錢單；Phase0 三閘之一（真 30 筆＋律師＋本人拍板）。
- 「優化器 0 晉升」自此**結案**：不需再每輪重新診斷；它是正確輸出，等樣本自然成熟即可，無待修程式碼。
