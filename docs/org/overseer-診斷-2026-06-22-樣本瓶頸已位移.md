# 監督員診斷 · 2026-06-22 中午 · 樣本瓶頸已從「OKX 帳戶設定」位移到「進場到期未成交」

> Layer 2 監督員第 7 輪喚醒產出。純唯讀診斷，未碰 daemon、未跨紅線。
> 目的：修正 oversight_ledger / 前 6 輪 L2 心跳一直複述的**過時 blocker**，把真正的當前瓶頸交給下一個 CEO Session。

## 一句話結論
模擬盤（demo）下單**早已不是「被 OKX 拒絕」卡住**——近期下單其實大多被接受了（13 筆進到 open/closed）。
真正卡住樣本成長的是：**這些限價單因為價格沒回到進場區、到期未成交（`entry_expired`，R=0）**。
這正是 task#61 Step B（執行半）要解的問題，而 Step B **尚未接線到執行路徑**。

## 證據（trade_journal.db · demo_trades，台北中午唯讀查詢）
- 全時段狀態分佈：`closed: 8`、`open: 5`、`rejected: 36`(累計，含早期 51010/51121/BadSymbol 年代)。
- **8 筆 closed 全部 `exit_reason=entry_expired`、`realized_r=0.0`**（LTC/MET/TRX/FIL/XPL/SOL/ATOM…）。
- 5 筆 open 是掛著還沒成交的限價單，依同樣型態極可能也會到期失效。
- 近期 rejected 已不是 51010：而是 `place_demo_plan 未成功`(generic) 與 `BadSymbol`(標的不在 OKX demo 永續宇宙，v86 prefilter 正在處理)。

## 程式碼確認（非僅推論）
- **Step A（學習半）= LIVE**：`entry_policy_optimizer` / `entry_policy_store` 已接到 `auto_tuner` + `paper_journal`。
  - `entry_policy_audit.jsonl` 最後一批 42 筆皆 `action: hold`（優化器有跑，但樣本太薄、無晉升）。
- **Step B（執行半）= 未接線**：`l3_dispatcher/demo_trader.py`、`demo_operator.py` 內**零** `entry_policy` / `to_market` / `convert` 參照。
  → 故 demo 限價單仍會到期失效，沒有「到期轉市價（D→market）」的救援。
- v90（f5a1b9b）的「entry_zone 單一權威委派」是純 refactor（intraday 值逐位元相同、零訊號變更），**不是** Step B。

## 為什麼監督員本輪不自己做 Step B
Step B = 把 `resolve_entry_policy` 接進 demo 執行路徑 + 到期限價單轉市價，屬**改 daemon 常駐執行模組**、且會實質改變成交行為（依 entry-depth AB 定案須走 champion/challenger 過 L2 統計閘才晉升）。
依監督員守則「不確定就不要動 daemon、改記任務交由人工」，此項**不適合無人值守自動落地**，交下一個 CEO Session 走 RB-1。

## 給下一個 CEO Session 的單一最高價值下一步
**task#61 Step B**：在 demo 執行路徑消費 `resolve_entry_policy`，並對 D 政策實作「到期未成交 → 轉市價」救援；
依 entry-depth-ab 定案，D 把涵蓋率 35%→94% 且零顯著 EV 損，是解 `entry_expired` 餓死樣本的鑰匙。
落地走 RB-1（停 watchdog→改→py_compile→相關測試→單次乾淨重啟→驗證→重啟 watchdog）。

## 仍屬使用者側、未變的 blocker
- 真錢實倉 0/30（紅線①，AI 永不自動下真錢單）——Phase0 三閘之一。
- OKX 模擬盤帳戶若仍有 51010，改單幣種/跨幣種保證金可清掉殘餘 `place_demo_plan 未成功`，但**已非主要瓶頸**。
