# 監督員診斷 · 2026-06-22 · entry_expired 仍是主要樣本流失，但 Step B 已接線只是「休眠」

> Layer 2 監督員第 7 輪喚醒產出。純唯讀診斷，未碰 daemon、未跨紅線。
> ⚠️ 本檔已於同輪內自我更正：初版誤稱「task#61 Step B 未接線」（因只 grep 了 demo_trader）。
> 正確事實見下。保留更正痕跡＝紅線③不掩蓋。

## 一句話結論（更正後）
`entry_expired`（限價單到期未成交、R=0）確實是當前**主要樣本流失來源**——
paper 37/114 closed（32%）、demo 8/8 closed 全是它。
但解它的 **task#61 Step B（到期限價單轉市價）早已接線且 LIVE**（在 paper_journal＋trade_monitor，非 demo_trader），
目前**休眠**：優化器 42 筆稽核全部 `action: hold`、**0 晉升**，故轉市價分支從未實際觸發。
真正待查＝**為何 114 筆 closed（> MIN_BUCKET_N=30）兩天了仍 0 晉升**。

## 證據（trade_journal.db，台北中午唯讀）
- paper_trades：closed 114 / open 24。closed 的 exit_reason＝`stop 39`、**`entry_expired 37`**、`tp3 21`、`timeout 17`。
- demo_trades：closed 8 / open 5 / rejected 36(累計)。**8 筆 closed 全部 `entry_expired` R=0**；近期 rejected 已非 51010，而是 `place_demo_plan 未成功`＋`BadSymbol`。
- entry_policy_audit.jsonl：42 筆全 `action: hold`，promote／coverage_promote 事件數＝**0**。

## 程式碼確認（更正初版錯誤）
- **Step B 已接線（與記憶 entry-depth-ab 一致）**：`paper_journal.py`＋`trade_monitor.py` 內可見
  `resolve_entry_policy` / `convert_pending_to_market` / `limit_convert` / `entry_policy_kind`。
  治本選擇刻意**不碰 macro.py / 也不在 demo_trader**，而在 paper 路徑自解析。初版只 grep demo_trader → 誤判「未接線」，特此更正。
- **休眠機制（inert-on-ship）**：convert 只在某分桶晉升 D（limit_convert）後才在生產觸發；晉升需 n≥30。目前 0 晉升 → convert 永不觸發 → 限價單照常 entry_expired。
- **demo 操盤手（OKX 模擬單）路徑確實未消費 entry_policy**——但 demo 是執行驗證軌、非 Phase0 解鎖閘，故不是 Phase0 的binding constraint。

## 真正的待查問題（交下一個 CEO Session）
**114 筆 closed paper 樣本 > MIN_BUCKET_N=30，為何 task#62/#63 的全域池(*,*)兩天仍 0 晉升？**
最可能＝優化器只計入 `entry_policy_kind` 對齊的樣本（此欄 2026-06-20 才加），故 6-20 之前的舊 closed 樣本 kind=None、不計入 → 有效對齊 n 仍 <30。
若屬實，瓶頸＝**對齊樣本累積速度**（自 6-20 起），而非缺程式碼。需讀 `entry_policy_optimizer.py` 的「對齊樣本」定義確認，屬須細讀常駐訊號模組的工作 → 監督員不無人值守處理，交 CEO Session。

## 為什麼監督員本輪不動 daemon
查清「對齊樣本」定義與任何修正都要讀／改 `entry_policy_optimizer`（常駐模組），且可能改變晉升行為（須走統計閘）。
依守則「不確定就不要動 daemon、改記任務交由人工」，交下一個 CEO Session 走 RB-1。本輪純唯讀＋文件＋推送。

## 仍屬使用者側、未變的 blocker（紅線①）
- 真錢實倉 0/30（AI 永不自動下真錢單）——Phase0 三閘之一（真30筆＋律師＋本人拍板）。
- OKX 模擬盤 51010 若仍在，改保證金模式可清殘餘 `place_demo_plan 未成功`，但**已非主要瓶頸**（demo 非 Phase0 閘）。
