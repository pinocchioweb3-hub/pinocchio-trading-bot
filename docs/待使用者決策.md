# 待使用者決策清單

> 由監督員（Layer 2）維護。**每輪重驗前提，不是重貼**——前提沒了就當場更正並移除，
> 不讓已經被你用行動回答過的問題靠慣性續命（r66 的教訓：曾拿一個早就推上 GitHub 的問題繼續追問）。
>
> 最後重驗：**2026-08-01 21:1x（+0800）**，監督員 r95。

---

## ① WLFI 孤兒倉：要不要人工平倉？（**唯一的真錢阻塞**）

**現況（本輪實測）**：`WLFI-USDT-SWAP long 11618 張`，交易所上有、本地部位帳沒有。
`last_fail_class` 仍是 `orphan_position`，累計 581 輪（上一輪 552、再上一輪 518）＝**每一輪都還在打**。

**這代表什麼**（別讀錯）：
- ⛔ 這**不是**「沒下單所以沒風險」。那是一筆**在場的真實曝險**。
- 它不受自動管理：不會逾時平倉、了結損益**不進**日/週熔斷口徑。
- 但它的**交易所端止損仍掛著**，所以不是裸奔。
- 管線其餘部分照常運作，只有**同幣同向**的新單被擋。

**為什麼要你決定**：全庫零 WLFI 痕跡 ⇒ 極可能是你自己手動開的倉。
偵測器在結構上分不出「使用者手動倉」與「脫帳的機器人腿」，所以它只能擋、不能自行處置。
**在它消失前，這條阻塞不會自己解除。**

**你的選項**：(a) 到 OKX 確認後手動平掉；(b) 保留它，並接受同幣同向新單持續被擋。

---

## ② 實盤金鑰與模擬盤鏡像，要不要共存？

**現況（本輪實測，未變）**：模擬盤操盤手旗標是開的，但每一輪都被 `demo_guard` 擋下 ⇒
**零新單，且既有部位停止對帳**（帳面數字會越來越舊，不可當現況讀）。

擋單原文：偵測到實盤交易金鑰已設定（`OKX_TRADE_API_KEY` / `_SECRET` / `_PASSPHRASE` 三槽皆有值）；
模擬盤模式下這三個必須全空，以杜絕對真錢下單。

**⛔ 這道閘的行為完全正確，永不弱化。** 它是「AI 絕不誤下真錢單」的實體防線。
（先前評估過的替代方案「閘擋時讓對帳先跑」已確認**做不到**：那條路必然要放寬實盤金鑰檢查＝弱化這道閘。
所以「對帳中斷」是這個決定的必然代價，不是可修的 bug。）

**你的選項**：(a) 清空／輪替 `OKX_TRADE_*` 讓模擬盤鏡像復活（真錢執行器那側會隨之停）；
(b) 維持現狀，接受模擬盤鏡像停擺。

**⚠️ 潛伏性提醒**：改 `.env` **要跨過一次重啟才算數**——7/29 那次是 03:33 寫入、17:41 重啟才引爆。

---

## ③ `platform/` 與 `docs/trust-site/` 要不要納入版控？（**本輪新增**）

**實測**：`git status` 顯示 `?? platform/`（37 檔 441K）與 `?? docs/trust-site/`；
`.gitignore` 沒有任何相關規則、也沒有巢狀 `.git` ⇒ 純粹**從來沒 commit 過**。
`git show HEAD:platform/api/data_access.py` 直接 fatal。

**代價**：這兩棵樹**零版本歷史、零還原點**。任何一次編輯（包含 AI 的）都無法 `git checkout --` 撤回。
而它偏偏是**對外信任頁**的 API 與前端——會呈現筆數與誠實聲明的地方。
（今天 v201 修的正是這裡的一個 bug：資料源壞掉時，誠實聲明會消失、筆數卻照送。）

**監督員已做的**：先把兩棵樹快照到
`%LOCALAPPDATA%\TradingBot\unversioned_snapshot\2026-08-01T1309Z\` 當還原點（v148 先例）。

**⛔ 監督員不會單方面把整棵未審子樹推上去**：這是 **PUBLIC** repo，
納管前必須逐檔審過 `config.example.json`、`run_platform.ps1`、`web/` 有無本機路徑、端點或任何識別資訊。

**你的選項**：(a) 授權監督員先做一次逐檔審查並提出「可公開／需先清理」清單給你過目，再由你決定納管；
(b) 維持未納版控，靠快照當還原點。

---

## 已解除，不必再問（留著防止復發追問）

- **IP 白名單 401**：8/01 02:32 起已解除。本輪 `auth_ip_whitelist` 計數仍凍在 **1551**（沒再長）＝仍是已解。
  ⛔ 判它有沒有復發**看計數有沒有再長**，不要看 `last_ok_ts`——有孤兒倉時那個時戳結構上不會前進。
- **未推送的 commit**：本輪 `git fetch origin` 後 `origin/main..HEAD` 為空＝全部已推。
