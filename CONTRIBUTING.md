# 貢獻指南

感謝你想讓這台機器人變得更強。本專案的貢獻有兩條路：

## 路線一：💡 意見箱（不用會寫程式）

加入 Telegram 社群後，在「💡 意見箱」主題發表建議即可。流程：

1. **人人可提**，全部留痕（≥10 字才送 AI 評估；每人每小時評估 1 則防洪水）
2. **AI 初評**：Claude 評估真實價值與可行性（空泛口號得低分，
   具體可做的建議得高分 — 例：「加入 XX 數據判斷 YY」優於「讓 AI 更聰明」）
3. **管理者審核**：高潛力建議（評分 ≥7）自動通知管理者
4. **採納並實裝後給分**：基礎 5 分，依貢獻度 1-10 分。
   更新公告會註明建議出自誰。

**積分規則**：
- 只看真實貢獻，不看發言量與字數
- 累積制**永不清零** — 貢獻者是這台開源機器人的合作股東
- 未來返佣分潤的 50% 依積分占比分配（詳見 [VISION.md](VISION.md)）

## 路線二：程式碼貢獻

1. Fork → 開 branch → 改動 → Pull Request
2. PR 請說明：解決什麼問題、怎麼驗證過（測試輸出貼進 PR）
3. 風格：跟隨現有程式碼（繁中註解、型別標註、單檔自帶 `__main__` 煙霧測試）
4. 紅線：
   - 任何金鑰 / 識別資訊不得進 repo（CI 會擋，審查也會擋）
   - 不接受移除風控（熔斷、曝險上限、靜默期）的 PR
   - 新數據源必須優雅降級（缺 key 時不能讓 daemon 崩潰）

被合併的 PR 同樣計入貢獻積分（依改動的真實價值評分）。

## 開發環境

```bash
pip install -r requirements.txt
cp .env.example .env   # 至少填 TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID
python -m pytest tests/ -x
python run_bot.py --backend mock --once   # 不需任何付費 API 的煙霧測試
```

## 行為準則

訊號頻道禁言（只放訊號與數據）；意見箱嚴禁謾罵與歧視。
我們用數據說話 — 對事不對人。
